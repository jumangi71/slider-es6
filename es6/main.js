import Slider from './Slider.js';

let slider = new Slider('.slider-container', {
  animation: 'slide',
  navigator: true,
  arrows: true,
  items: 4
});

//var body = document.querySelector('body');
//body.textContent = 'Good point: ' + new Slider(1, 23);
